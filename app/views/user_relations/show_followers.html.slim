.wrapper.clearfix
  h2
    |フォローされているユーザー
  - @user.followers.each do |follower|
    .user_list
      p.user_name
        = link_to "#{follower.profile.screen_name}", profile_path(follower.profile.id)
      p.user_introduction
        = follower.profile.introduction
