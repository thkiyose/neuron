<% flash.each do |type, message| %>
  $(".notifications").html('<div class="<%= type %>"><%= message %></div>');  
<% end %>
$("#favorites_button_"+"<%= @contribution.id %>").html("<%= escape_javascript(render partial: 'favorites/favorites_button', locals: { contribution: @contribution } ) %>")
